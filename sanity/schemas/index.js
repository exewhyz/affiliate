import product from "./product.schema";
import productPlaylist from "./productPlaylist.schema";
import categories from "./categories.schema";

const schemas = [product, productPlaylist, categories];

export default schemas;
